# Patches are applied with `git apply` by scripts/prepare-rustpush.mjs against
# LF sources. A Windows checkout that rewrote them to CRLF would fail to apply,
# so keep their bytes verbatim on every platform.
*.patch -text
