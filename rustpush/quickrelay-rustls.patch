diff --git a/Cargo.toml b/Cargo.toml
index 4d47f25..fb5d184 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -19,7 +19,7 @@ uuid = { version = "1.4.1", features = ["v4"] }
 base64 = "0.21.2"
 regex = "1.9.3"
 tokio-rustls = "0.26.4"
-rustls = "0.23.38"
+rustls = "0.23.28"
 rustls-pemfile = "1.0.3"
 rand = { version = "0.8.5", features = ["min_const_gen"] }
 libflate = "2.0.0"
@@ -56,19 +56,18 @@ aes-siv = "0.7.0"
 chrono = "0.4.41"
 bitflags = "2.9.1"
 x509-cert = "0.2.5"
-rustls_psk = { package = "rustls", path = "../../rustls/rustls" }
-quinn = { path = "../../quinn/quinn" }
+quinn = { path = "../quinn-qpod/quinn" }
 h3 = "0.0.8"
 h3-quinn = "0.0.10"
 http = "1.4.0"
-rtc-stun = { path = "../../rtc/rtc-stun" }
-rtc-shared = { path = "../../rtc/rtc-shared" }
-rtc-rtp = { path = "../../rtc/rtc-rtp" }
-rtc-srtp = { path = "../../rtc/rtc-srtp" }
-rtc-media = { path = "../../rtc/rtc-media" }
+rtc-stun = { path = "../rtc-openbubbles/rtc-stun" }
+rtc-shared = { path = "../rtc-openbubbles/rtc-shared" }
+rtc-rtp = { path = "../rtc-openbubbles/rtc-rtp" }
+rtc-srtp = { path = "../rtc-openbubbles/rtc-srtp" }
+rtc-media = { path = "../rtc-openbubbles/rtc-media" }
 
 [patch.crates-io]
-quinn = { path = "../../quinn/quinn" }
+quinn = { path = "../quinn-qpod/quinn" }
 
 [target.'cfg(target_os = "macos")'.dependencies]
 core-foundation = "0.9.4"
diff --git a/src/ids/link.rs b/src/ids/link.rs
index bc5889e..e01a702 100644
--- a/src/ids/link.rs
+++ b/src/ids/link.rs
@@ -39,7 +39,7 @@ const TOPIC: &'static str = "com.apple.private.alloy.facetime.multi";
 
 #[derive(Debug)]
 struct NoCertificateVerification;
-use rustls_psk::{
+use rustls::{
     CertificateError, ClientConfig, Error, KeyLogFile, SignatureScheme, client::{PresharedKeySet, danger::{HandshakeSignatureValid, ServerCertVerified, ServerCertVerifier}}, crypto::PresharedKey
 };
 
@@ -59,8 +59,8 @@ impl ServerCertVerifier for NoCertificateVerification {
             &self,
             message: &[u8],
             cert: &CertificateDer<'_>,
-            dss: &rustls_psk::DigitallySignedStruct,
-        ) -> Result<rustls_psk::client::danger::HandshakeSignatureValid, Error> {
+            dss: &rustls::DigitallySignedStruct,
+        ) -> Result<rustls::client::danger::HandshakeSignatureValid, Error> {
         Ok(HandshakeSignatureValid::assertion())
     }
 
@@ -68,8 +68,8 @@ impl ServerCertVerifier for NoCertificateVerification {
             &self,
             message: &[u8],
             cert: &CertificateDer<'_>,
-            dss: &rustls_psk::DigitallySignedStruct,
-        ) -> Result<rustls_psk::client::danger::HandshakeSignatureValid, Error> {
+            dss: &rustls::DigitallySignedStruct,
+        ) -> Result<rustls::client::danger::HandshakeSignatureValid, Error> {
         Ok(HandshakeSignatureValid::assertion())
     }
     
@@ -886,7 +886,7 @@ pub struct GlobalLink {
 
 impl GlobalLink {
     async fn connect_to_relay(response: &QuickRelayAllocationsResponse, incoming: tokio::sync::mpsc::Sender<GlobalPacket>) -> Result<(quinn::Connection, SendRequest<h3_quinn::OpenStreams, rasn::prelude::OctetString>), PushError> {
-        let mut client_crypto: ClientConfig = rustls_psk::ClientConfig::builder()
+        let mut client_crypto: ClientConfig = rustls::ClientConfig::builder()
             .dangerous()
             .with_custom_certificate_verifier(Arc::new(NoCertificateVerification))
             .with_no_client_auth();
@@ -908,9 +908,15 @@ impl GlobalLink {
         let server_name = ServerName::IpAddress(IpAddr::V4(Ipv4Addr::from(target_ip)));
 
         let mut keys = PresharedKeySet::default();
+        let hash = rustls::crypto::aws_lc_rs::cipher_suite::TLS13_AES_128_GCM_SHA256
+            .tls13()
+            .expect("TLS 1.3 cipher suite")
+            .common
+            .hash_provider;
         keys.update(server_name.clone(), Arc::new([PresharedKey::external(
             &identity, 
-            &expanded_key
+            &expanded_key,
+            hash,
         ).unwrap()]));
         client_crypto.preshared_keys = Arc::new(keys);
 
diff --git a/src/test.rs b/src/test.rs
index d443ca3..9a0a772 100644
--- a/src/test.rs
+++ b/src/test.rs
@@ -690,7 +690,7 @@ pub fn decode_hex(s: &str) -> Result<Vec<u8>, ParseIntError> {
 
 #[tokio::main(worker_threads = 12)]
 async fn main() {
-    rustls_psk::crypto::ring::default_provider().install_default().expect("Failed to install rustls crypto provider");
+    rustls::crypto::ring::default_provider().install_default().expect("Failed to install rustls crypto provider");
 
     if let Err(_) = std::env::var("RUST_LOG") {
         std::env::set_var("RUST_LOG", "debug");
